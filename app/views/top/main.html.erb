ログイン中…
<br>
<% @tweets.each do |e| %>
    <p>
        <%= e.message %>
        (<%= e.user_id %>)
        <%=link_to '削除', "/top/#{e.id}", data: { turbo_method: :delete } %>
    </p>
<% end %>

<%= link_to '新規追加', '/top/new' %>

<%= link_to 'ログアウト', top_logout_path %>